alias c="clear"
alias cls="clear"

alias chx="chmod +x"

alias ..="cd .."
alias ...="cd ../.."

#alias lt='ls --human-readable --size -1 -S --classify'
alias lt='du -sh * | sort -h'
alias left='ls -t -1'

alias x="exit"

mkcd() { mkdir -p "$@" && cd "$@"; }
