# homebrew https://gist.github.com/indiesquidge/ec010eca3ffa254788c2
alias brewup='brew update; brew upgrade; brew prune; brew cleanup; brew doctor'

# alias chrome="/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome"
alias cls="clear"

# pow & vpn: https://github.com/basecamp/pow/issues/524
alias fix_pf="sudo pfctl -f /etc/pf.conf; sudo pfctl -e"

alias t="todo.sh -d ~/.todo.cfg"
alias reload=". ~/.zshrc"
alias x="exit"

alias weather='curl wttr.in/Baltimore'

alias mnt='mount | grep -E ^/dev | column -t'
alias count='find . -type f | wc -l'
alias cpv='rsync -ah --info=progress2'

alias idea='env $(cat .env | xargs) open -a "`ls -dt /Applications/IntelliJ\ IDEA*|head -1`" $*'

# https://www.mokacoding.com/blog/env-xargs/
#alias load_dotenv='env $(cat .env | grep -v "#" | xargs) $*'
#alias load_dotenv='env $(grep -v '^#' .env | xargs)' $*
